apiVersion: apps/v1
kind: Deployment
metadata:
  name: osrm-server
  namespace: experiments
  labels:
    app: osrm-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: osrm-server
  template:
    metadata:
      labels:
        app: osrm-server
    spec:
      imagePullSecrets:
      - name: github-registry
      containers:
      - name: osrm-server
        image: ghcr.io/project-osrm/osrm-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
          protocol: TCP
        command: ["osrm-routed"]
        args:
          - "--algorithm"
          - "mld"
          - "/data/california-latest.osrm"
        envFrom:
        - configMapRef:
            name: osrm-env
        volumeMounts:
        - name: osrm-data
          mountPath: /data
          readOnly: true
        resources:
          requests:
            memory: "2Gi"
            cpu: "1"
          limits:
            memory: "25Gi"
            cpu: "5"
      volumes:
      - name: osrm-data
        hostPath:
          path: /infra/k8s-manifests/experiments/osrm/data/processed
          type: Directory