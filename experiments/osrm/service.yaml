apiVersion: v1
kind: Service
metadata:
  name: osrm-server-nodeport
  namespace: experiments
  labels:
    app: osrm-server
spec:
  type: NodePort
  selector:
    app: osrm-server
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 32050
    protocol: TCP
    name: http