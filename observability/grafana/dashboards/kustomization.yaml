apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

configMapGenerator:
- name: grafana-dashboard-k8s-node-status
  files:
  - k8s-node-status.json
  options:
    labels:
      grafana_dashboard: "1"

generatorOptions:
  disableNameSuffixHash: true